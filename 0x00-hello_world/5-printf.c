#include <stdio.h>

/**
 * main - A program that prints a line with the print f
 *
 * Return: 0 when successful
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art");
	return (0);
}
