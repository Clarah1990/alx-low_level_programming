#include <stdio.h>
/**
 * main - Program that prints the alphabet in lowercase, followed by a new line
 * Return: Always 0 (Success)
 */
int main(void)
{
	char i;

	for (i = 'a''\n' ; i <= 'z''\n' ; i++)
		putchar(i);
	return (0);
}
