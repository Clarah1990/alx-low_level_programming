#include "main.h"

/**
 * _isupper - function that checks for uppercase character
 * @c: char to check
 * Return: 1 if c is uppercase
 * and 0 otherwise
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= '2')
		return (1);
	else
		return (0);
}
